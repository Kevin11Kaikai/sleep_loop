# Clean Root Receipt

- `CLEAN_ROOT = PASS`
- `IMPORT_ALLOWLIST = PASS` for the seven-file selective v2 list only
- `PROTECTED_ACCESS_IN_V4D1 = NONE`
- `ROLE_ISOLATION = PASS`

R4 began in a fresh execution-only context, initialized its denylist and access/role ledgers before repository scientific inspection, and has not read, listed, previewed, hashed, or executed Night-2, protected-derived, sealed, or fresh-final scientific content. The historical execution bundle failed closure and is denied; only the independently audited per-file subset is authorized.
