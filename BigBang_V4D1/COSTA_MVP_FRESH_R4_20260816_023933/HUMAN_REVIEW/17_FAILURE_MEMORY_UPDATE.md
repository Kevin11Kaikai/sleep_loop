# Failure memory update

Retain these governance lessons without changing the independent judgment:

- Import execution bundles only after transitive closure review; default-deny and exact per-file allowlisting prevented the defective historical runner bundle from entering R4.
- Distinguish intended path identity from recorded hash identity and freeze corrections before outcome execution.
- If an acknowledgement is scientifically required, freeze an explicit ACK gate; otherwise record timing races precisely.
- Close operational state records contemporaneously and cryptographically chain audit entries when stronger forensic guarantees are needed.
- Describe environment closure at the level actually checked, not as exhaustive OS-handle proof.
- Raw-open budgets must close explicitly; R4 is exhausted and no further access is authorized.

Qualified Incident-001 statement: R4 itself had no protected access. Before the V3 denylist, one protected-derived cross-night JSON reached the V3 Root/Curator; the raw/final-fresh bank was not opened. No Incident-001 payload or exact value is reproduced here.
