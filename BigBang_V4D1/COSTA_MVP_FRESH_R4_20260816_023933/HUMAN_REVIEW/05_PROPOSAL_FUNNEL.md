# Proposal funnel

The campaign entered with one primary mechanistic claim, M1, and optional C1. All mandatory gates G1-G7 passed, so M1 became eligible at `C0/P0` with `DISCOVERY_PLUS_INDEPENDENT_REPRODUCTION` evidence.

C1 remains `NOT_ASSIGNED`: synthetic coupling specificity was used as gate evidence, and no separate optional C1 adjudication was requested or executed. M2 and C2 are `NOT_ELIGIBLE/NOT_AUTHORIZED`; P1 and P2 are `NOT_ELIGIBLE/NOT_AUTHORIZED`.

No optional claim is promoted from gate evidence, and no broader claim is substituted for the narrow primary result.
