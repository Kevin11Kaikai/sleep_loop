# Data and compute feasibility

Authorized empirical scope was exactly the SC4001 Sleep-EDF Expanded cassette Night-1 PSG/hypnogram pair. The deterministic upstream-labeled N3 partition contained 220 eligible epochs: 110 FIT and 110 HELDOUT, with zero excluded in either partition.

For each of the two authorized EDFs, accounting closed at 3/3 logical operations: one provenance SHA-256 read, one worker-steward MNE logical read, and one independent-verifier MNE logical read. Remaining budget is zero; status is `EXHAUSTED_CLOSED`. This is the prospectively defined logical-operation budget, not an OS-handle count.

All scientific stages had one orchestrated invocation, with no scientific retry or outcome-guided repair. No further raw or protected access is authorized.
